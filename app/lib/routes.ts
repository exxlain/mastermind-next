export enum Routes {
    LOGIN = "/login",
    SIGNUP = "/signup",
    REGISTER = "/register",
    GAME = "/game",
    SCORE = "/score",
    HOME = "/",
}

export enum SrcLinks {
    BULL = "/bull.svg",
}
